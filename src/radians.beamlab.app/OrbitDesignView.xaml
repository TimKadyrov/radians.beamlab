<UserControl x:Class="radians.beamlab.app.OrbitDesignView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- left: inputs + case previews -->
        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,8,0">
            <GroupBox Header="Target orbit">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/><RowDefinition/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Altitude (km)"/>
                    <TextBox   Grid.Row="0" Grid.Column="1" Text="{Binding TargetAltitudeKm, UpdateSourceTrigger=LostFocus}"/>
                    <TextBlock Grid.Row="1" Text="Inclination (deg)"/>
                    <TextBox   Grid.Row="1" Grid.Column="1" Text="{Binding InclinationDeg, UpdateSourceTrigger=LostFocus}"/>
                    <TextBlock Grid.Row="2" Text="Eccentricity"/>
                    <TextBox   Grid.Row="2" Grid.Column="1" Text="{Binding Eccentricity, UpdateSourceTrigger=LostFocus}"/>
                    <TextBlock Grid.Row="3" Text="Max orbits / cycle"/>
                    <TextBox   Grid.Row="3" Grid.Column="1" Text="{Binding MaxOrbitsPerCycle, UpdateSourceTrigger=LostFocus}"/>
                    <TextBlock Grid.Row="4" Text="Search band (km)"/>
                    <TextBox   Grid.Row="4" Grid.Column="1" Text="{Binding SearchBandKm, UpdateSourceTrigger=LostFocus}"/>
                </Grid>
            </GroupBox>

            <TextBlock Text="{Binding StatusText}" Foreground="#666666" TextWrapping="Wrap"/>

            <GroupBox Header="Case 1 — free drift (artificial precession)">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="130"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="NOrbits (run)"/>
                        <TextBox Grid.Column="1" Text="{Binding NOrbits, UpdateSourceTrigger=LostFocus}"/>
                    </Grid>
                    <TextBlock Text="{Binding Case1Text}" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Case 2 — station-kept repeating track">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="130"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="keep_rnge (deg)"/>
                        <TextBox Grid.Column="1" Text="{Binding KeepRangeDeg, UpdateSourceTrigger=LostFocus}"/>
                    </Grid>
                    <TextBlock Text="{Binding Case2Text}" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#1a1a1a"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding KeepRangeValid}" Value="False">
                                        <Setter Property="Foreground" Value="#b00020"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="Case 3 — declared precession">
                <TextBlock Text="{Binding Case3Text}" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap"/>
            </GroupBox>

            <Button Content="Copy SNS fields" Click="OnCopyClick"/>
        </StackPanel>
        </ScrollViewer>

        <!-- right: solutions grid + ground track -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="200"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DataGrid Grid.Row="0" ItemsSource="{Binding Solutions}"
                      SelectedItem="{Binding SelectedSolution, Mode=TwoWay}"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                      Background="#ffffff" Foreground="#1a1a1a" BorderBrush="#cccccc"
                      RowBackground="#ffffff" AlternatingRowBackground="#f6f8f7">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="orbits k" Binding="{Binding Orbits}" Width="70"/>
                    <DataGridTextColumn Header="days m" Binding="{Binding NodalDays}" Width="60"/>
                    <DataGridTextColumn Header="exact alt (km)" Binding="{Binding AltitudeText}" Width="100"/>
                    <DataGridTextColumn Header="delta (km)" Binding="{Binding DeltaText}" Width="80"/>
                    <DataGridTextColumn Header="cycle" Binding="{Binding CycleText}" Width="100"/>
                    <DataGridTextColumn Header="drift@target (deg/cyc)" Binding="{Binding DriftText}" Width="140"/>
                    <DataGridTextColumn Header="spacing (deg)" Binding="{Binding SpacingText}" Width="95"/>
                    <DataGridTextColumn Header="max keep_rnge" Binding="{Binding MaxKeepText}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Row="1" Margin="0,8,0,0" BorderBrush="#cccccc" BorderThickness="1" Background="#fbfdfc">
                <Grid>
                    <Canvas x:Name="TrackCanvas" ClipToBounds="True"/>
                    <TextBlock Text="{Binding TrackClosureText}" Margin="8,4"
                               VerticalAlignment="Bottom" HorizontalAlignment="Left"
                               Foreground="#666666" FontFamily="Consolas" FontSize="11"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
