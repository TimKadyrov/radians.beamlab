<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <!-- vendored radlimits (epfdshare/) carries unsafe GPU-layout structs -->
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <!-- radians defines WIN64 in every configuration; the vendored
         radlimits picks the 64-bit BR DLL name by it -->
    <DefineConstants>$(DefineConstants);WIN64</DefineConstants>
    <RootNamespace>radians.beamlab</RootNamespace>
    <AssemblyName>radians.beamlab.core</AssemblyName>
    <InvariantGlobalization>true</InvariantGlobalization>
  </PropertyGroup>

</Project>
